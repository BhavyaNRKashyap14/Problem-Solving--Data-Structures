Description
Nth Fibonacci Number is calculated using this formula. It is sum of previous two numbers.

Fib(N) = Fib(N-1) + Fib(N-2)

Zeroth And First Fibonacci Numbers are 0 and 1.

Given N, print Nth Fibonacci Number.

Prohibited Keywords
CPP : while,for

JAVA : while,for

PYTHON : while,for

JAVASCRIPT : while,for

C# : while,for

Input Format
Few numbers are given, One Per Line.

Output Format
For Every Number N, Print Nth Fibonacci Number on a Separate Line.

Constraints
0 <= N <= 15

Example
Input 1
3
5
11
 
Output 1
2
5
89
 
Explanation 1
Fibonacci Series is 


------------------------------------------------------------------------------------------
#include <bits/stdc++.h>
using namespace std;
class BeingZero{
	public:
	long long fibonacci(int n)
	{
	    if(n == 0)
	    {
	        return 0;
	    }
	    else if((n == 1) || (n==2))
	    {
	        return 1;
	    }
	    return fibonacci(n-1)+fibonacci(n-2);
		// Just complete given function
		// Do not write main or input reading or output printing
		// That is already taken care by hidden code    
	}
};


--------------------------------------------------------------------------------------------
